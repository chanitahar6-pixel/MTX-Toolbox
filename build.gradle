// Top-level build file. Groovy DSL on purpose: no Kotlin anywhere in this project.
plugins {
    id 'com.android.application' version '8.5.2' apply false
}

tasks.register('clean', Delete) {
    delete rootProject.layout.buildDirectory
}
